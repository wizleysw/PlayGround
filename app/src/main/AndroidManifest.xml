<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wizley.android.playground">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".components.activity.data.parcelable.ParcelableActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".components.activity.launchmode.StandardActivity"
            android:launchMode="standard"/>
        <activity
            android:name=".components.activity.launchmode.SingleTaskActivity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".components.activity.launchmode.SingleTopActivity"
            android:launchMode="singleTop"/>
        <activity
            android:name=".components.activity.launchmode.SingleInstanceActivity"
            android:launchMode="singleInstance"/>

        <activity android:name=".components.activity.activitymove.ktNavigateToActivity"/>
    </application>

</manifest>